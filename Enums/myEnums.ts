enum SeatChoise {
  AISLE = "aisle", // we can initialize a value for a enum item what we want to call it
  MIDDLE = 0,
  WINDOW,
  FOURTH,
}

const Aseat = SeatChoise.AISLE;
console.log(Aseat);


export{}
